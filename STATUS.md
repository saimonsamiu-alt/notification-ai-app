# প্রজেক্ট স্ট্যাটাস (এই ফাইলটা সবসময় আপডেট রাখা হবে)

## এখন পর্যন্ত যা হয়েছে
- ফেজ ১ সম্পন্ন: Email (IMAP) + Telegram (Bot API) fetcher + rule-based importance filter + CLI dashboard
- ফেজ ২ সম্পন্ন: `llm_classifier.py` — Ollama (লোকাল LLM) দিয়ে স্মার্ট ক্লাসিফিকেশন, না থাকলে rule-based-এ auto-fallback
- ফেজ ১+২ কোড GitHub-এ পুশ করা: github.com/saimonsamiu-alt/notification-ai-app
- ফেজ ৩ সম্পন্ন: `reply_templates.py` + `reply_logger.py` (হ্যাশ-চেইন সহ, tamper-evident)
- `main.py`-তে জরুরি মেসেজের জন্য রিপ্লাই সাজেশন + লগিং যোগ হয়েছে (এখনো আসল প্ল্যাটফর্মে পাঠানো হয় না, শুধু লগ হয়)
- `data/` ফোল্ডার `.gitignore`-এ যোগ করা হয়েছে
- **সিকিউরিটি ফিচার যোগ হয়েছে (৩টা):**
  1. `integrity_check.py` — কোড ফাইলের SHA-256 manifest, চালু হওয়ার সময় স্বয়ংক্রিয় যাচাই, মিসম্যাচ হলে safe mode
  2. `reply_logger.py`-তে হ্যাশ-চেইন — পুরানো লগ এন্ট্রি বদলালে ধরা পড়ে (টেস্ট করে যাচাই করা হয়েছে, কাজ করছে)
  3. `safety_monitor.py` — রেট-লিমিট/অ্যানোমালি ডিটেকশন, অস্বাভাবিক পরিমাণ auto-action হলে safe mode-এ চলে যায়
- `integrity_manifest.json` জেনারেট করা হয়েছে বর্তমান কোডের জন্য — **⚠ নতুন কোড আপডেট করার পর অবশ্যই `python -m notification_agent.integrity_check --generate` আবার চালাতে হবে, নাহলে নিজের কোডকেই "corrupted" ভাববে**
- ইউজার Ollama মডেল (llama3.2:3b) ডাউনলোড করে ফেলেছে, এখন টেস্ট করবে

## পরের ধাপ (এখান থেকে চালিয়ে যেতে হবে)
1. Ollama ডাউনলোড শেষ হলে ইউজার ফেজ ১+২+৩ একসাথে টেস্ট করবে
2. রিপ্লাই লগ কিছুদিন জমলে QLoRA দিয়ে ছোট মডেল ফাইন-টিউন করার কোড লিখতে হবে (ফেজ ৩-এর বাকি অংশ)
3. এরপর ফেজ ৪: অ্যাভাটার ইন্টারফেস (ভয়েস + টেক্সট) — এখানেই আসল রিপ্লাই পাঠানোর ফিচারও (SMTP/Telegram send) যোগ হবে
4. বড় কোডবেস (Android + ল্যাপটপ একসাথে) ম্যানেজ করার জন্য Claude Code ব্যবহার করা ভালো, ইউজারকে সাজেস্ট করা হয়েছে

## গুরুত্বপূর্ণ সিদ্ধান্ত (ভুলে গেলে এখানে দেখো)
- সব প্রসেসিং লোকাল থাকবে, কোনো ক্লাউড না (প্রাইভেসি + খরচ কম রাখার জন্য)
- WhatsApp/Instagram এখনো যোগ করা হয়নি — ঝুঁকিপূর্ণ, ইউজারকে জানিয়ে পরে যোগ হবে
- সবসময় ফ্রি/ওপেন-সোর্স টুল আগে ট্রাই করা, খরচ লাগলে আগে জিজ্ঞেস করা
- টার্গেট: স্টুডেন্ট, বিজনেস পার্সন, অফিশিয়াল — বাংলাদেশ/ইন্ডিয়া মার্কেট
- অ্যাপ ভয়েস+টেক্সট দুটোই সাপোর্ট করবে, সেটআপ কখনো লম্বা হবে না
- টোকেন এফিশিয়েন্সি: ভারী কোডিং Claude Code-এ, চ্যাটে প্ল্যানিং + ছোট আপডেট

## গুরুত্বপূর্ণ সিদ্ধান্ত (ভুলে গেলে এখানে দেখো)
- সব প্রসেসিং লোকাল থাকবে, কোনো ক্লাউড না (প্রাইভেসি + খরচ কম রাখার জন্য)
- WhatsApp/Instagram এখনো যোগ করা হয়নি — ঝুঁকিপূর্ণ, ইউজারকে জানিয়ে পরে যোগ হবে
- সবসময় ফ্রি/ওপেন-সোর্স টুল আগে ট্রাই করা, খরচ লাগলে আগে জিজ্ঞেস করা
- টার্গেট: স্টুডেন্ট, বিজনেস পার্সন, অফিশিয়াল — বাংলাদেশ/ইন্ডিয়া মার্কেট
- অ্যাপ ভয়েস+টেক্সট দুটোই সাপোর্ট করবে, সেটআপ কখনো লম্বা হবে না
