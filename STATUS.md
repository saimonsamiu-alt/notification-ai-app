# প্রজেক্ট স্ট্যাটাস (এই ফাইলটা সবসময় আপডেট রাখা হবে)

## এখন পর্যন্ত যা হয়েছে
- ফেজ ১ সম্পন্ন: Email (IMAP) + Telegram (Bot API) fetcher + rule-based importance filter + CLI dashboard
- ফেজ ২ সম্পন্ন: `llm_classifier.py` — Ollama (লোকাল LLM) দিয়ে স্মার্ট ক্লাসিফিকেশন, না থাকলে rule-based-এ auto-fallback
- ফেজ ১+২ কোড GitHub-এ পুশ করা: github.com/saimonsamiu-alt/notification-ai-app
- ফেজ ৩ সম্পন্ন: `reply_templates.py` + `reply_logger.py` (হ্যাশ-চেইন সহ, tamper-evident)
- `main.py`-তে জরুরি মেসেজের জন্য রিপ্লাই সাজেশন + লগিং যোগ হয়েছে (এখনো আসল প্ল্যাটফর্মে পাঠানো হয় না, শুধু লগ হয়)
- `data/` ফোল্ডার `.gitignore`-এ যোগ করা হয়েছে
- **সিকিউরিটি ফিচার যোগ হয়েছে (৩টা):**
  1. `integrity_check.py` — কোড ফাইলের SHA-256 manifest, চালু হওয়ার সময় স্বয়ংক্রিয় যাচাই, মিসম্যাচ হলে safe mode
  2. `reply_logger.py`-তে হ্যাশ-চেইন — পুরানো লগ এন্ট্রি বদলালে ধরা পড়ে (টেস্ট করে যাচাই করা হয়েছে, কাজ করছে)
  3. `safety_monitor.py` — রেট-লিমিট/অ্যানোমালি ডিটেকশন, অস্বাভাবিক পরিমাণ auto-action হলে safe mode-এ চলে যায়
- `integrity_manifest.json` জেনারেট করা হয়েছে বর্তমান কোডের জন্য — **⚠ নতুন কোড আপডেট করার পর অবশ্যই `python -m notification_agent.integrity_check --generate` আবার চালাতে হবে, নাহলে নিজের কোডকেই "corrupted" ভাববে**
- ইউজার Ollama মডেল (llama3.2:3b) ডাউনলোড করে ফেলেছে, এখন টেস্ট করবে
- **টেস্ট ফলাফল (ইউজারের নিজের ল্যাপটপে):**
  - Email fetching + LLM classification (Ollama) সফলভাবে কাজ করছে ✅
  - Telegram এখনো কাজ করছে না — ইউজারের ISP/নেটওয়ার্ক সরাসরি Telegram সার্ভারের IP ব্লক করে রেখেছে (browser দিয়ে কাজ করে, raw socket/Python দিয়ে করে না — DNS ঠিকই resolve হয়, কিন্তু TCP connect timeout)। এটা কোডের সমস্যা না। সমাধান: সিস্টেম-লেভেল VPN লাগবে (শুধু ব্রাউজার এক্সটেনশন না)। **আপাতত পেন্ডিং রাখা হয়েছে, ইউজারের সিদ্ধান্তে।**
  - ফিল্টার একটু বেশি sensitive ছিল (routine Facebook নোটিফিকেশনকেও জরুরি মার্ক করছিল) — `llm_classifier.py`-র প্রম্পটে rules যোগ করে ঠিক করা হয়েছে, ইউজারের মেশিনে টেস্ট এখনো বাকি
  - `TIMEOUT_SECONDS` মান ইউজার লোকালি 15→60 (LLM) এবং 10→30 (Telegram) করেছিল, এখন source-এও মিলিয়ে দেওয়া হয়েছে
- **ফেজ ৭ থেকে এগিয়ে আনা হলো, তারপর রিডিজাইন করা হয়েছে:** `best_picker.py` — শুধু "সেরাটা বাছাই" না, বরং একই ক্যাটাগরিতে (fund request, idea ইত্যাদি) একাধিক মেসেজ থাকলে প্রতিটাকে AI দিয়ে ১-১০ স্কোর করে মান-যাচাই করে, তারপর সেরা থেকে সর্বনিম্ন সাজায়। থ্রেশহোল্ডের (৬/১০) নিচের মেসেজ "রিভিউ করার মতো না" হিসেবে আলাদা দেখায়। ১০০টা মেসেজে ০টাও "ভালো" না হতে পারে, আবার অনেকগুলোও হতে পারে — সংখ্যা জোর করে নির্ধারণ করে না। `main.py`-তে dashboard-এর পরপরই দেখায়।
- **ব্যক্তিগতকৃত rubric + Compare Mode যোগ হয়েছে:**
  - `rubric_manager.py` — প্রথমবার চালানোর সময় ইউজারের/টিচারের নিজের মাপদণ্ড (academic merit, financial need, clarity, extracurricular — প্রতিটা ১-১০ গুরুত্ব) মাইক্রো-স্টেপে জিজ্ঞেস করে `data/rubric.json`-এ সেভ করে, পরের বার আর জিজ্ঞেস করে না
  - `best_picker.py`-এর র‍্যাংকিং এখন এই rubric অনুযায়ী স্কোর করে (rubric না থাকলেও কাজ করে, সাধারণ বিচারে)
  - `compare_mode.py` — ইউজার চাইলে ২-৩টা মেসেজ সরাসরি পাশাপাশি তুলনা করে দেখতে পারে (rubric-ভিত্তিক), `main.py`-তে র‍্যাংকিং দেখানোর পর ঐচ্ছিকভাবে জিজ্ঞেস করে
  - এই তিনটাই এখনো ইউজারের মেশিনে টেস্ট করা বাকি
- **পার্সোনা-অ্যাওয়ারনেস + উষ্ণ feedback acknowledgment যোগ হয়েছে:**
  - `persona_manager.py` — প্রথমবার চালানোর সময় (rubric-এর প্রশ্নের সাথেই) জিজ্ঞেস করে ইউজার student/business/official কিনা, সেভ করে রাখে
  - `llm_classifier.py`-র প্রম্পট এখন persona-context নিয়ে ক্লাসিফাই করে (যেমন স্টুডেন্টের জন্য একাডেমিক ডেডলাইন জরুরি, সোশ্যাল মিডিয়া নোটিফিকেশন কম জরুরি — এভাবে টার্গেট অনুযায়ী বদলায়)
  - `main.py`-র `handle_replies`-এ এখন ইউজার সাজেশন skip/edit করলে উষ্ণভাবে স্বীকার করে ("ঠিক আছে, বুঝেছি — মনে রাখলাম"), বিরক্তিকর একগাদা প্রশ্ন না করে। প্রতি ৩ বার feedback-এ একটা ছোট সারাংশ দেখায় যে সে শিখছে ও আরও কিছু বদলানো দরকার কিনা জিজ্ঞেস করে
- **ফাইন-টিউনিং স্ক্রিপ্ট (`train.py`) বানানো হয়েছে — প্রজেক্টের প্রথম আনটেস্টেড অংশ:**
  - `python -m notification_agent.train --check` — কতটা রিপ্লাই ডেটা জমেছে দেখায় (এটা টেস্ট করা হয়েছে, কাজ করে)
  - `python -m notification_agent.train` — QLoRA দিয়ে (unsloth লাইব্রেরি, Llama 3.2 3B বেস মডেল) ফাইন-টিউন করে, GGUF এক্সপোর্ট করে, Ollama-র জন্য Modelfile বানিয়ে দেয় — **⚠ এটা GPU ছাড়া টেস্ট করা যায়নি, ইউজারের মেশিনে প্রথমবার চালানোর সময় সমস্যা হতে পারে**
  - ন্যূনতম ৫০টা লগ করা রিপ্লাই লাগবে ট্রেনিং শুরু করতে (এখনও ০টা আছে — ইউজারকে অ্যাপ ব্যবহার করে যেতে হবে)
  - `requirements-training.txt` আলাদা রাখা হয়েছে (unsloth, torch ইত্যাদি ভারী প্যাকেজ, শুধু ট্রেনিং-এর সময় লাগবে, দৈনন্দিন ব্যবহারে না)

## পরের ধাপ (এখান থেকে চালিয়ে যেতে হবে)
1. Ollama ডাউনলোড শেষ হলে ইউজার ফেজ ১+২+৩ একসাথে টেস্ট করবে
2. রিপ্লাই লগ কিছুদিন জমলে QLoRA দিয়ে ছোট মডেল ফাইন-টিউন করার কোড লিখতে হবে (ফেজ ৩-এর বাকি অংশ)
3. এরপর ফেজ ৪: অ্যাভাটার ইন্টারফেস (ভয়েস + টেক্সট) — এখানেই আসল রিপ্লাই পাঠানোর ফিচারও (SMTP/Telegram send) যোগ হবে
4. বড় কোডবেস (Android + ল্যাপটপ একসাথে) ম্যানেজ করার জন্য Claude Code ব্যবহার করা ভালো, ইউজারকে সাজেস্ট করা হয়েছে

## গুরুত্বপূর্ণ সিদ্ধান্ত (ভুলে গেলে এখানে দেখো)
- সব প্রসেসিং লোকাল থাকবে, কোনো ক্লাউড না (প্রাইভেসি + খরচ কম রাখার জন্য)
- WhatsApp/Instagram এখনো যোগ করা হয়নি — ঝুঁকিপূর্ণ, ইউজারকে জানিয়ে পরে যোগ হবে
- সবসময় ফ্রি/ওপেন-সোর্স টুল আগে ট্রাই করা, খরচ লাগলে আগে জিজ্ঞেস করা
- টার্গেট: স্টুডেন্ট, বিজনেস পার্সন, অফিশিয়াল — বাংলাদেশ/ইন্ডিয়া মার্কেট
- অ্যাপ ভয়েস+টেক্সট দুটোই সাপোর্ট করবে, সেটআপ কখনো লম্বা হবে না
- টোকেন এফিশিয়েন্সি: ভারী কোডিং Claude Code-এ, চ্যাটে প্ল্যানিং + ছোট আপডেট

## গুরুত্বপূর্ণ সিদ্ধান্ত (ভুলে গেলে এখানে দেখো)
- সব প্রসেসিং লোকাল থাকবে, কোনো ক্লাউড না (প্রাইভেসি + খরচ কম রাখার জন্য)
- WhatsApp/Instagram এখনো যোগ করা হয়নি — ঝুঁকিপূর্ণ, ইউজারকে জানিয়ে পরে যোগ হবে
- সবসময় ফ্রি/ওপেন-সোর্স টুল আগে ট্রাই করা, খরচ লাগলে আগে জিজ্ঞেস করা
- টার্গেট: স্টুডেন্ট, বিজনেস পার্সন, অফিশিয়াল — বাংলাদেশ/ইন্ডিয়া মার্কেট
- অ্যাপ ভয়েস+টেক্সট দুটোই সাপোর্ট করবে, সেটআপ কখনো লম্বা হবে না
